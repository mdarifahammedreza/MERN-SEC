/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["assets.aceternity.com", "images.unsplash.com"], // Add your external image hostnames here
      },
};

export default nextConfig;
